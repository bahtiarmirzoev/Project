<UserControl x:Class="Project.View.PreviewPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project.View"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:viewmodel="clr-namespace:Project.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:PreviewPageViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Image Source="https://i.imgur.com/Kz65T4p.png">
            <Image.Effect>
                <BlurEffect Radius="10"/>
            </Image.Effect>
        </Image>


        <Button
            Command="{Binding Register}"
            Name="RegistrationBtn" Width="150" Background="Orange" Margin="0,136,0,0" HorizontalAlignment="Center" VerticalAlignment="Top">Registration</Button>
        <Button
            Command="{Binding Login}"
            Name="LoginBtn" Width="150" Background="Orange" Margin="0,264,0,0" HorizontalAlignment="Center" VerticalAlignment="Top">Login</Button>
    </Grid>
</UserControl>





